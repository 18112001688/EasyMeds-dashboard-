import 'package:flutter/material.dart';

class AcceptedOrDeclinedOrdersView extends StatelessWidget {
  const AcceptedOrDeclinedOrdersView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
